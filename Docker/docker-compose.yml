version: '2'

services:

    php:
        container_name:  youtool-auth-bundle-php
        tty: true
        restart: 'no'
        build:
            context: ./Php
        volumes:
            - ./../:/var/app:cached
